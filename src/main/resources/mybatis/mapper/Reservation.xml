<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.tset.rentalCar.reservation.mapper.ReservationMapper">

	<insert id="insertReservation">
		insert into tb_reservation (re_total_price,re_date,re_status, user_no,car_no) 
		values (#{reTotalPrice}, #{dateTime}, '예약완료', #{userNo}, #{carNo})
	</insert>

</mapper>